// feed.js — placeholder until posts exist.
// Once the backend exists, fetch the real feed like:
//
// async function loadFeed() {
//   const res = await fetch('/api/posts');
//   const posts = await res.json();
//   renderPosts(posts);
// }
// loadFeed();

console.log('Feed loaded. No posts to render yet — the empty state is intentional.');
